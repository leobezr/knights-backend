ACCOUNT
   - account creation
   - session
CHARACTER
   - VOCATION
      - Knight
      - Archer
      - Wizzard
   - FOOD
      - food system (regen, potion)
      - buff system 
   - honor system @done
   - level / exp
      - (level) => Math.round(50 * Math.pow(level, 3) / 3) - Math.round(100 * Math.pow(level, 2)) + Math.round(850 * Math.pow(level, 1) / 3) - 200
DATABANK
   - choose data base @done
      - MongoDB (NoSQL) @done
DOCKER
   - TODO
OTHERS
   - websocket
   - EQUIPS
      - unequip function @done
      - throw equip away function @done
ITEMS
   - STORE @done
      - item value @done
      - sell item @done
         - selling: (goldValue * tier) * .1 @done
         - buying: (goldValue * tier) / .1 @done
            - selling(30000, 10) = 30000 || selling(100, 1) = 10 @done
            - buying(30000, 10) = 3000000  || buying(100, 1) = 1000 @done
