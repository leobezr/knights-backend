CHARACTER
   - FOOD
      - food system (regen, potion)
      - buff system 
   - honor system
   - level
      - (level) => Math.round(50 * Math.pow(level, 3) / 3) - Math.round(100 * Math.pow(level, 2)) + Math.round(850 * Math.pow(level, 1) / 3) - 200
DATABANK
   - choose data base @done
      - MongoDB (NoSQL) @done
DOCKER
   - TODO
OTHERS
   - EQUIPS
      - unequip function @done
      - throw equip away function @done
   - ACCOUNT
      - account creation
      - session
   - websocket
ITEMS
   - STORE
      - item value @done
      - sell item
         - selling: (goldValue * tier) * .1
         - buying: (goldValue * tier) / .1
            - selling(30000, 10) = 30000 || selling(100, 1) = 10
            - buying(30000, 10) = 3000000  || buying(100, 1) = 1000
